import React from 'react';

const twitterBanner = (props) => (
  <img id="twitterBanner" src={props.twitterPic} style={{ maxWidth: '100%', maxHeight: '100%' }} alt="hello world" />
)

export default twitterBanner;